# تقرير إصلاح وتشغيل Shadow Bot

**المشروع:** Shadow Bot  
**المستودع:** [kalix-c/Shadow][1]  
**بيئة النشر المستهدفة:** Replit  
**إعداد التقرير:** Manus AI  
**التاريخ:** 2026-08-16

## الملخص التنفيذي

تم فحص مشروع Shadow، وإعادة تثبيت اعتمادياته، وتشغيل اختبارات التحقق، واختبار خادم الصحة محلياً على المنفذ `3000`. أظهرت عملية الفحص وجود مشكلة تهيئة عند التشغيل الأول لملف قاعدة بيانات المجموعات، كما أظهرت محاولة التثبيت المحلية على Node.js 22 حاجة اعتماد `canvas` إلى مكتبات Cairo وPango ومكتبات الصور أثناء البناء من المصدر. عولجت المشكلة الأولى في الكود، وعولجت الثانية في إعداد Nix الخاص بـ Replit.

> **ملاحظة أمنية:** لم يتم إدراج ملف `ShadowSetUp/ShadowState.json` أو أي رموز جلسة أو أسرار أو ملفات بيئة في هذا الالتزام. الملف مطلوب للتشغيل الفعلي فقط، ويجب إبقاؤه محلياً أو حفظه في Secrets/Storage آمن وفق آلية النشر المستخدمة.

## المشكلات المكتشفة والإصلاحات

| # | المشكلة | السبب | الإصلاح المنفذ | الملف المتأثر |
|---:|---|---|---|---|
| 1 | قد يفشل التشغيل الأول عند استيراد متحكم المجموعات | كان الكود يقرأ `database/threads.json` مباشرة ثم يمرر محتواه إلى `JSON.parse` قبل ضمان وجود الملف أو احتوائه على مصفوفة JSON صالحة | تمت إضافة `ensureFileSync`، وقراءة الملف كنص، واستخدام مصفوفة فارغة عند كون الملف جديداً أو فارغاً | `src/database/controllers/threads.controllers.js` |
| 2 | عدم اتساق اعتماد `fs` في وحدة تنزيل الملفات | الوحدة تستخدم `createWriteStream` ولا تحتاج واجهات `fs-extra` الإضافية | تم توحيد الاستيراد إلى `node:fs` لتقليل الاعتماد غير الضروري في هذا المسار | `helper/utils.js` |
| 3 | قد يفشل بناء اعتماد `canvas` في بيئات Nix التي لا تحتوي مكتباته الأصلية | محاولة `npm ci` على Node.js 22 وصلت إلى بناء `canvas` من المصدر وفشلت بسبب عدم توفر Cairo عبر `pkg-config` | تمت مواءمة `replit.nix` مع Node.js 20 المستخدم في `.replit`، وإضافة `pkg-config` وCairo وPango ومكتبات JPEG وGIF وSVG المطلوبة للبناء | `replit.nix` |

## التحقق والاختبارات

| الاختبار | النتيجة | التفاصيل |
|---|---|---|
| `npm ci --ignore-scripts` | ناجح | تم تثبيت حزم JavaScript بنجاح مع تجاوز خطوات البناء الأصلية مؤقتاً لعزل أخطاء الكود |
| `npm test` | ناجح | نجح فحص الصياغة واستيراد الوحدات الأساسية، وأفاد الفحص بسلامة `214` ملف JavaScript |
| `npm run health` | ناجح | بدأ خادم الصحة على المنفذ `3000` |
| `curl http://127.0.0.1:3000/` | ناجح | أعاد حالة JSON: `Shadow Bot` بحالة `running` |
| `git diff --check` | ناجح | لا توجد أخطاء مسافات أو تعارضات تنسيق في التغييرات |
| فحص الملفات الحساسة المتتبعة | ناجح | لا يوجد `ShadowState.json` أو `.env` أو مفاتيح أو رموز ضمن الملفات المتتبعة |

## الملفات التي لم تُرفع

استُبعدت ملفات الجلسة والبيئة والأسرار من Git عبر `.gitignore`. لا ينبغي نسخ محتوى جلسة Messenger إلى مستودع عام أو مشاركته في السجل؛ وإذا كان أي رمز جلسة قد ظهر خارج بيئة التشغيل الموثوقة، فمن الأفضل إبطاله أو تدويره وإنشاء جلسة جديدة.

## ملاحظات التشغيل على Replit

يستخدم ملف `.replit` الأمر `npm run start` ويعرّف المنفذ المحلي `3000`. يتطلب التشغيل الفعلي للبوت وجود ملف جلسة Messenger صالح في `ShadowSetUp/ShadowState.json` داخل بيئة Replit، إضافة إلى تثبيت الاعتماديات بنجاح. أما خادم الصحة فيعمل مستقلاً عن جلسة Messenger ويمكن استخدامه للتحقق من أن العملية تستمع على المنفذ الصحيح.

لم يُعتبر نجاح خادم الصحة دليلاً على نجاح تسجيل الدخول إلى Messenger؛ فهما مساران مختلفان. يجب اعتبار جلسة Messenger اختباراً منفصلاً، ويجب عدم تسجيل محتوى الجلسة في السجلات أو رفعه إلى GitHub.

## حالة الالتزام

تشمل التغييرات الآمنة المرفوعة:

- إصلاح تهيئة `threads.json` عند أول تشغيل.
- توحيد استيراد `fs` في وحدة الأدوات.
- تحديث مكتبات البناء في `replit.nix` للتوافق مع اعتماد `canvas` وNode.js 20.
- إضافة هذا التقرير إلى المستودع.

## مراجع

[1]: https://github.com/kalix-c/Shadow "مستودع Shadow على GitHub"
[2]: https://replit.com/@mohamedchaouni0/Shadow "مشروع Shadow على Replit"
